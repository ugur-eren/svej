import {View} from 'react-native';
import {<%= props.name %>Props} from './<%= props.name %>.props';

const <%= props.name %>: React.FC<<%= props.name %>Props> = (props) => {
  return (
    <View>
      <View />
    </View>
  );
};

export default <%= props.name %>;
