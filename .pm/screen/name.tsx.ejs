import {View} from 'react-native';
import {Text} from '../../Components';
import {PageContainer} from '../../Containers';
import {useLanguage, useTheme} from '../../Hooks';
import getStyles from './<%= props.name %>.styles';

// TODO: Add your screen props here.
type Props = {};

const <%= props.name %>: React.FC<Props> = ({navigation}) => {
  const theme = useTheme();
  const language = useLanguage();

  const styles = getStyles(theme);

  return (
    <PageContainer withSafeArea withPadding>
      <View style={styles.container}>
        <Text fontSize={24}>{language.routes.<%= props.nameCamelCase %>.title}</Text>
      </View>
    </PageContainer>
  );
};

export default <%= props.name %>;
